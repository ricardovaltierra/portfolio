import styles from '../style';

const Contact = () => (
  <section className={`${styles.section}`}>
    <p className="">
      🚧️ In construction 🚧️
    </p>
  </section>
);

export default Contact;
